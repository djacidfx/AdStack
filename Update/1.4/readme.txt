Please replace the files inside "application" folder.

**New database import is mandatory, so import the "database.sql" file. Make sure to take a backup first.
**We will not be responsible for any data loss.